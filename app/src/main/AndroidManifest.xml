<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recipe_Search"
        tools:targetApi="31">

        <!-- Main Activity (Launcher Activity) -->
        <activity
            android:name="com.college.final_project.RecipeSearchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Favorite Activity -->
        <activity
            android:name="com.college.final_project.FavoriteActivity"
            android:exported="true">
            <!-- If you have specific intent-filters for this activity, add them here -->
        </activity>


        <!-- Recipe Details -->
        <activity
            android:name="com.college.final_project.RecipeDetailsActivity"
            android:exported="true">
            <!-- If you have specific intent-filters for this activity, add them here -->
        </activity>



    </application>

</manifest>
